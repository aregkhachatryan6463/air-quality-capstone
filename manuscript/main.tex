% Capstone thesis / paper draft — compile with:
%   pdflatex main
%   bibtex main
%   pdflatex main
%   pdflatex main
% (from this directory; requires booktabs, natbib, hyperref, xcolor)

\documentclass[11pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage[round,authoryear]{natbib}

\hypersetup{colorlinks=true,linkcolor=blue!50!black,citecolor=blue!50!black,urlcolor=blue!50!black}

\title{Short-Term PM\textsubscript{2.5} Forecasting in Yerevan:\\
A Comparative Study of Statistical and Machine Learning Models}

\author{%
Areg Khachatryan\\[0.5em]
\small American University of Armenia\\
\small \textit{Capstone project} --- Supervisor: Rafayel Shirakyan}

\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Fine particulate matter (PM\textsubscript{2.5}) is a leading environmental risk factor for morbidity and mortality in urban areas. This study addresses \emph{when} parsimonious time-series models (AR / SARIMA) versus machine learning (ensembles, DeepAR) are appropriate for \emph{short-term} (one- to four-hour) hourly PM\textsubscript{2.5} forecasting in Yerevan, Armenia, and how conclusions depend on spatial aggregation (city mean vs administrative districts vs station-level samples) and on temporal stability. After aligning data to a regular grid and controlled imputation (with optional sensitivity to medium-gap policy), we use leakage-safe features including lags, rolling moments, and calendar encodings, with strict chronological train/validation/test splits. \emph{Walk-forward} evaluation (primary interpretive frame) reports mean error and across-fold variance for a common model zoo, including SARIMA in each fold. Point metrics on a single test window are complemented by circular \emph{block} bootstrap CIs for MAE and Diebold--Mariano tests with a data-driven HAC lag. A deep probabilistic DeepAR path reports 90\% coverage and a Winkler score for interval quality. A recent AUA capstone monograph on air quality in Yerevan \citep{auaair2025yerevan} offers useful local context; the present work extends the empirical record with a fully specified ML pipeline, spatial analysis, and robust validation. The repository is reproducible.
\end{abstract}

\noindent\textbf{Keywords:} air quality; PM\textsubscript{2.5}; time series forecasting; Diebold--Mariano test; machine learning; Yerevan

\section{Introduction}

Ambient PM\textsubscript{2.5} concentrations are associated with cardiovascular and respiratory harm even below levels that produce visible smog \citep{world2021who}. In many cities, policymakers and the public benefit from timely forecasts that support health advisories, traffic measures, and communication during pollution episodes. Yerevan experiences recurring wintertime pollution linked to residential heating, topography, and meteorology; reliable short-horizon forecasts are therefore both scientifically interesting and practically relevant.

Forecasting PM\textsubscript{2.5} at hourly resolution is challenging because concentrations are persistent, seasonal, and driven by nonlinear interactions between emissions and weather. A substantial literature applies machine learning (ML) and deep learning to air quality prediction \citep{shao2020predictions}, often reporting impressive accuracy. At the same time, classical time-series models remain standard benchmarks \citep{box2015time,hyndman2021forecasting}, and comparative studies must guard against information leakage, overfitting to a single temporal split, and overinterpretation of point estimates without uncertainty.

We study four interlinked questions: (1) \emph{When} do linear autoregressive and seasonal (SARIMA) models outperform feature-rich ML? (2) How does \emph{spatial aggregation} (city, district, station) change model rankings? (3) Do tuned boosting and DeepAR alter qualitative conclusions? (4) Are differences \emph{stable over time} under walk-forward validation? We emphasize reproducibility: frozen AR coefficients, explicit split dates, block bootstrap and HAC-consistent Diebold--Mariano tests \citep{diebold1995comparing,harvey1997testing}, and code-level leakage control for new features.

\paragraph{Contributions.}
\begin{itemize}
  \item We implement a modular pipeline: controlled imputation audit, past-only lags, rolling features, and optional spatial re-aggregation to districts (metadata-based or, if needed, data-driven $k$).
  \item We compare baselines, frozen AR(24), auto-SARIMA, Ridge, and gradient-boosted trees with Hyperopt, plus optional DeepAR with probabilistic metrics.
  \item \emph{Walk-forward} (expanding) evaluation with SARIMA in each fold and fold-wise error variance is the primary basis for ``stability'' claims, alongside a single hold-out for headline tables.
  \item We contrast city vs district \emph{vs} station-level weighted errors; permutation importance and train-set ACF/PACF help explain why linear dynamics can dominate.
\end{itemize}

\subsection{Institutional context and open data}

Air quality measurements in Armenia are disseminated through the national open-data portal associated with \texttt{airquality.am}. City-average hourly series aggregate information across monitoring stations and are a pragmatic target for public communication because they summarize typical exposure conditions at urban scale. Such aggregation smooths micro-scale heterogeneity (street canyons, traffic corridors) that station-level models would emphasize; our results should therefore be read as \emph{city-representative} forecasts rather than site-specific nowcasts. From a public-health perspective, short-term forecasts can complement threshold-based alerts even when they are not causal predictors of health outcomes \citep{eea2019air}.

\subsection{Organization}

Section~\ref{sec:related} reviews forecasting evaluation and ML applications in air quality. Section~\ref{sec:data} describes the dataset, imputation, and chronological splits. Section~\ref{sec:methods} specifies preprocessing, features, model classes, and inferential tools. Section~\ref{sec:results} presents empirical accuracy, bootstrap intervals, and Diebold--Mariano tests. Section~\ref{sec:discussion} interprets findings; Section~\ref{sec:limitations} lists limitations; Section~\ref{sec:conclusion} concludes. Appendix~\ref{app:hyper} tabulates hyperparameters for reproducibility.

\section{Related work}
\label{sec:related}

\paragraph{Forecast comparison and inference.}
\citet{diebold1995comparing} formalized tests for equal forecast accuracy; finite-sample adjustments and loss differentials are discussed by \citet{harvey1997testing}. We follow a long-run HAC variance for the mean loss differential (Bartlett--weighted autocovariances) with a lag order tied to the forecast horizon and a Newey--West-style rule, reported alongside each test.

\paragraph{Time-series and machine learning for air quality.}
Surveys highlight the diversity of ML approaches applied to PM\textsubscript{2.5} forecasting, from shallow learners to deep networks \citep{shao2020predictions}. Classical ARIMA/SARIMA models remain common benchmarks because they encode smooth persistence and seasonality \citep{box2015time}. \citet{hyndman2021forecasting} provide accessible treatment of seasonal models, cross-validation for series, and forecast evaluation.

\paragraph{Direct versus iterated multi-step forecasting.}
\citet{marcellino2006comparison} compare direct and iterated multistep autoregressive forecasts; direct methods estimate separate models for each horizon, whereas iterated methods chain one-step models. We employ direct multi-step estimation for horizons $h\in\{1,2,3,4\}$ hours for supervised models, which trades off model proliferation against simpler error propagation.

\paragraph{Local comparators.}
The AUA CSE monograph on air quality in Yerevan \citep{auaair2025yerevan} situates PM\textsubscript{2.5} sources, health context, and monitoring. Our contribution is methodological (forecasting protocol, feature audit, and spatial robustness) using an updated hourly panel; readers should not equate the two works' modeling targets or data vintages.

\paragraph{Interpretability.}
We complement accuracy tables with train-segment ACF/PACF plots, permutation-based importance for tuned XGBoost on a held test slice, and a district/station error comparison where sample sizes allow; impurity importances are \emph{not} primary \citep{lundberg2017unified}.

\section{Data and study area}
\label{sec:data}

\subsection{Source and variables}

We use open hourly air quality data for Yerevan distributed through the project data pipeline (see \texttt{DATA\_README.md} in the repository). The modeling target is city-average PM\textsubscript{2.5} ($\mu$g/m$^3$). Auxiliary variables used when sufficiently complete include PM\textsubscript{10}, temperature, pressure, and humidity. Timestamps are aligned to a regular hourly grid; gaps in the raw record motivate the imputation strategy discussed below.

\subsection{Data quality and imputation}

Real sensors exhibit downtime, calibration drift, and extreme spikes. The main pipeline uses \emph{controlled} imputation: very short runs of missing hours are filled with local temporal interpolation; longer medium-length gaps use hour-of-day medians; very long gaps are left missing where uncertainty is high, with binary flags for downstream use. A limited ffill/bfill (with a short cap) is applied only as a last resort for remaining covariate holes. This policy avoids naive forward-filling across large multi-day gaps. Missingness is audited (fractions, gap lengths, monthly patterns) and summarized in \texttt{results/json/results\_summary.json}. Complementary exploratory diagnostics (heatmaps, stationarity checks) can be generated with \texttt{scripts/data\_quality\_supplement.py} and should be summarized descriptively in revisions of this draft; that script uses a simpler fill for exploration and should not be confused with the main pipeline’s imputation module.

From a modeling standpoint, missingness in winter months may be \emph{not missing at random} if instrument outages correlate with extreme cold or maintenance schedules; we do not claim a formal MAR/MNAR identification result, but we flag the risk that imputation can bias performance toward optimistic values during problematic periods.

\subsection{Train, validation, and test splits}

We use a single chronological split with proportions 70\%/15\%/15\% of usable rows after listwise deletion of rows with missing targets or features. Validation is reserved for descriptive monitoring; the main results concern the \emph{test} period.

For the replication run underlying the tables in this manuscript, split boundaries and row counts match \texttt{results/json/split\_info.json} (also embedded in \texttt{results\_summary.json}). Ranges are as follows (from the last pipeline run):
\begin{itemize}
  \item Training: 2019-02-02 00:00 through 2023-12-27 20:00 (42{,}899 hourly rows in the model table).
  \item Validation: 2023-12-27 21:00 through 2025-01-13 21:00 (9{,}193 rows).
  \item Test: 2025-01-13 22:00 through 2026-01-31 22:00 (9{,}193 rows).
\end{itemize}
The test interval spans more than twelve months and includes at least one full heating season (October--April in the Northern Hemisphere), which matters for PM\textsubscript{2.5} dynamics in Yerevan.

A single hold-out is efficient for large hourly panels but does not average over multiple non-overlapping evaluation windows. The code implements an \emph{expanding} walk forward with a configurable test window and step, refitting a common zoo each fold. By default, SARIMA (auto order on each training segment) and linear baselines are included; gradient-boosted parameters can either be re-tuned per fold (``expensive'' option) or reuse validation-tuned values from the main 70/15/15 run (default). Per-fold DeepAR retraining is optional. Fold-wise means and variances, and a comparison table against the chronological test split, are in \texttt{results/tables/}.

\section{Methodology}
\label{sec:methods}

\subsection{Preprocessing and outliers}

We optionally cap extreme PM\textsubscript{2.5} values at a high quantile (main experiments: 99.5th percentile) to limit the influence of single spikes; sensitivity to this choice can be audited via the project's sensitivity sweep script. A rolling interquartile range rule is used to \emph{count} flagged hours for transparency, not to drop rows automatically in the main pipeline.

\subsection{Features}

The supervised models use a configurable set of \emph{strictly past} target lags, trailing rolling means/std/min/max, optional trigonometric seasonality (diurnal/annual), hour-of-day--lag interactions with met covariates when available, and short-horizon volatility (differences, trailing variance). Calendar features and a \emph{heating season} indicator (October--December, January--April) encode domain knowledge. All rolling statistics use a one-hour shift from the target hour to avoid look-ahead.

\subsection{Models}

\paragraph{Baselines.}
Persistence uses the current concentration as the one-step-ahead forecast. Seasonal naive at horizon one uses the value 24 hours earlier. A 24-hour rolling mean provides a smooth short-memory benchmark.

\paragraph{Autoregressive benchmark.}
We estimate an AR($p$) model with intercept on the \emph{training} segment only ($p=24$ in the main run) and generate one-step-ahead forecasts on later segments using fixed coefficients. This mirrors a leakage-avoidant forecasting exercise: the AR parameters are not re-estimated using test information.

\paragraph{Seasonal ARIMA class (software benchmark).}
The replication code also supports a seasonal SARIMA/SARIMAX-style benchmark with seasonal period 24 hours and order selection on the training sample; rolling one-step forecasts follow the same chronological principle as the AR benchmark. If enabled in the run, its performance appears alongside other models in the CSV outputs; the tables excerpted here focus on the models listed in Table~\ref{tab:main_results}.

\paragraph{Supervised learning.}
Ridge regression uses $\alpha=1$ with a stable solver. Default random forests, XGBoost, and LightGBM follow the repository’s baseline settings. Tree models can be tuned on the \emph{validation} window using Hyperopt (Tree-structured Parzen estimators) with mean absolute error as the inner objective; tuned parameters are reported in \texttt{results\_summary.json} and model names use a \texttt{\_tuned} suffix. The multilayer perceptron was removed from the active pipeline in favor of a clearer benchmark set and optional DeepAR for deep learning; older tables that list an MLP should be regenerated.

\subsection{Multi-horizon forecasting}

For horizons $h\in\{2,3,4\}$, we estimate \emph{separate} regressors for each horizon (direct multi-step), retraining on the corresponding shifted target. This differs from recursively chaining one-step models; both approaches are defensible \citep{marcellino2006comparison}, and we document the choice explicitly.

\subsection{Evaluation metrics and statistical testing}

Point forecast accuracy is summarized with MAE, RMSE, and $R^2$ on the test set. Let $y_t$ denote realized PM\textsubscript{2.5} at test hour $t$ and $\hat{y}_t^{(m)}$ the forecast from model $m$. Mean absolute error is $\mathrm{MAE}_m = T^{-1}\sum_t |y_t - \hat{y}_t^{(m)}|$.

\paragraph{Diebold--Mariano testing.}
For two models $A$ and $B$, define the loss differential $d_t = |y_t - \hat{y}_t^{(A)}| - |y_t - \hat{y}_t^{(B)}|$ under absolute-error loss. Under regularity conditions, the Diebold--Mariano statistic is approximately standard normal when scaled by a consistent long-run variance estimate for $\{d_t\}$ \citep{diebold1995comparing}. We implement a Newey--West style variance with one lag (a pragmatic choice for hourly data with strong serial correlation in errors). The null hypothesis is equal expected loss; two-sided $p$-values are reported. Following the reporting rule emphasized in the forecasting literature, we treat differences as \emph{practically meaningful} only when accompanied by $p<0.05$ or clearly separated bootstrap MAE intervals.

\paragraph{Bootstrap MAE intervals.}
Bootstrap confidence intervals for MAE are generated by resampling test hours with replacement (2{,}000 replications in the exported tables), holding each fitted model fixed. These intervals are i.i.d.\ rather than block bootstrap; they are adequate as a descriptive uncertainty band but understate temporal dependence in the error series and should not be interpreted as simultaneous confidence sets across all models.

\subsection{Sensitivity analysis (preprocessing)}

The codebase supports sensitivity sweeps over capping rules and lag sets by reusing the same \texttt{run\_pipeline} entry point. Reporting a full grid search is beyond the scope of this draft; the main text fixes the 99.5th percentile cap and lag set $\{1,2,3,24\}$ hours. Readers verifying robustness should consult the repository's sensitivity outputs and confirm that qualitative rankings are stable under reasonable alternatives.

\section{Results}
\label{sec:results}

\subsection{One-hour-ahead accuracy}

Table~\ref{tab:main_results} reports test-set performance for the one-hour horizon. The frozen AR(24) model attains the lowest MAE among all entries, followed by persistence and then the gradient-boosted and ridge specifications. The ordering illustrates that strong linear persistence---and an honestly specified AR benchmark---sets a high bar for ML in this setting.

\input{../paper/tables/table_main_results.tex}

\subsection{Uncertainty: bootstrap intervals}

Table~\ref{tab:bootstrap_mae} summarizes bootstrap 95\% intervals for MAE. When the reported pattern is as in the table, intervals for AR(24) and persistence can sit well below those of ridge, random forest, and XGBoost (and LightGBM, when that model is included in the run). Regenerate the table with \texttt{export\_paper\_assets.py} after each full pipeline run so the TeX matches \texttt{results/json/results\_summary.json}.

\input{../paper/tables/table_bootstrap_mae_ci.tex}

\subsection{Diebold--Mariano tests}

The pipeline writes paired tests to \texttt{results/tables/diebold\_mariano\_test.csv}. The default reporting rule keeps a small set of comparisons (e.g., best model versus the second-ranked model and selected linear benchmarks) to limit multiplicity. For the replication summarized in the exported CSV with AR(24)\_frozen as the best one-step model, examples include: AR(24)\_frozen versus persistence (larger $|\mathrm{DM}|$ under absolute loss) and AR(24)\_frozen versus ridge, with $n$ equal to the number of valid paired test hours. Exact $z$-style statistics and two-sided $p$-values in that file should be cited in place of any older narrative tied to a different best model (e.g.\ XGBoost--ridge) from an earlier run.

When AR(24) is best, a typical pattern is that differences against persistence and Ridge favor AR(24) on absolute error at conventional significance; consult the current CSV and \texttt{results\_summary.json} for numbers after each run.

\subsection{Multi-horizon behavior}

Figure~\ref{fig:mae_horizon} summarizes test MAE across horizons. Persistence errors grow with horizon as expected; supervised models remain clustered, with tree and linear models trading ranks depending on $h$. Readers should interpret multi-horizon results with the same caution about a single temporal split.

\begin{figure}[t]
\centering
% pdfTeX: avoids loading graphicx on installs missing grfext. Full TeX~Live users may switch to \texttt{\textbackslash includegraphics}.
\IfFileExists{../images/mae_by_horizon.png}{%
\pdfximage width 0.92\linewidth {../images/mae_by_horizon.png}\pdfrefximage\pdflastximage
}{%
  \fbox{\parbox{0.92\linewidth}{\small\noindent
  \textbf{Figure.} Place \texttt{../images/mae\_by\_horizon.png} here (run \texttt{run\_pipeline.py} and \texttt{export\_paper\_assets.py}).}}
}
\caption{Test mean absolute error by forecast horizon (direct multi-step, subset of models). File \texttt{images/mae\_by\_horizon.png} (or \texttt{results/plots/}).}
\label{fig:mae_horizon}
\end{figure}

\subsection{Chronological test versus walk-forward (auxiliary)}

In addition to the main test window, the code runs an expanding walk-forward design on a subset of one-step models and writes \texttt{results/tables/validation\_chronological\_vs\_walkforward\_1h.csv}, with a parallel block in \texttt{results\_summary.json}. The intent is a \emph{stability} check, not a second identically defined leaderboard.

\subsection{Diagnostic figures}

Time-series and bar plots for the best one-step model on the test set (e.g.\ \texttt{model\_performance\_1h\_ci.png}, \texttt{forecast\_vs\_actual\_best\_1h.png}) live under \texttt{results/plots/} and \texttt{images/}. For tree models, impurity-based importance (when saved) should be read alongside permutation-based alternatives in future work \citep{lundberg2017unified}.

\section{Discussion}
\label{sec:discussion}

The empirical pattern---a strong AR benchmark and competitive persistence---is consistent with high short-run autocorrelation in hourly PM\textsubscript{2.5}. When the conditional mean is close to a linear function of recent lags, penalized linear models and shallow trees may not unlock large gains relative to well-specified autoregressions, especially when flexibility is constrained by default hyperparameters chosen to limit overfitting.

Machine learning methods can approximate nonlinearities and interactions, but they also require careful tuning and can overfit when signal-to-noise is modest relative to model complexity. Under the chosen defaults, ridge and tree ensembles perform similarly on point MAE, and the Diebold--Mariano test does not separate XGBoost from ridge at conventional levels. This finding is scientifically useful: it suggests that marginal complexity above ridge is not automatically rewarded on this split without more extensive model selection on validation data (which would need its own inferential care to avoid optimistic bias).

Domain features such as the heating-season indicator encode scientifically meaningful structure; their contribution should be interpreted alongside meteorology and emissions context. The random-forest importance plot in the replication bundle emphasizes lags and calendar variables, which aligns with intuition about diurnal activity and seasonal heating. Impurity-based importance should, however, be supplemented with permutation importance in future revisions \citep{lundberg2017unified}.

Comparisons to literature values must respect differences in stations, horizons, loss functions, and leakage controls; headline MAE numbers are not directly comparable across studies without harmonization. European-scale monitoring and policy framing \citep{eea2019air} provide qualitative context for why accurate short-term forecasts matter even when long-term regulatory compliance is assessed on different statistics.

\section{Limitations}
\label{sec:limitations}

\begin{itemize}
  \item \textbf{Single hold-out.} The primary comparison uses one chronological test window; walk-forward error is an auxiliary stability check, not a second independent test of all models.
  \item \textbf{Imputation.} Results are conditional on the short-, medium-, and long-gap imputation policy; the supplement script uses a different fill for exploration only.
  \item \textbf{Deep and deep probabilistic baselines.} Optional DeepAR is included when dependencies are available; 90\% interval coverage, when reported, is a descriptive check, not a formal calibration test.
  \item \textbf{Walk-forward scope.} Default walk-forward refits a subset of models; hyperparameters for tuned trees are from the main validation search, not refit per fold.
  \item \textbf{Bootstrap.} Hourly errors are serially dependent; i.i.d.\ bootstrap intervals are optimistic for simultaneous inference.
  \item \textbf{Causal interpretation.} Forecast accuracy does not identify causal effects of policies or interventions.
\end{itemize}

\section{Conclusion}
\label{sec:conclusion}

We presented a transparent, reproducible comparison of statistical and machine learning models for short-term PM\textsubscript{2.5} forecasting in Yerevan. Under a strict chronological protocol with frozen autoregressive coefficients and paired statistical tests, a classical AR(24) benchmark delivered the best average absolute accuracy on the held-out test window, and differences between ridge and gradient boosting were not statistically significant at the 5\% level. These results caution against inferring ML superiority from point metrics alone and motivate reporting uncertainty and stronger validation designs in air quality forecasting work.

Future work includes nested hyperparameter control under walk-forward, permutation-based feature importance, optional structural-break robustness around the COVID-19 period, and archiving a frozen dataset snapshot with a permanent DOI for full reproducibility.

\appendix

\section{Hyperparameters and implementation defaults}
\label{app:hyper}

Table~\ref{tab:hyper} lists the principal settings used in the main experiments. Where multiple values are plausible, we adopt fixed defaults as controlled baselines rather than reporting an extensive tuning study; this choice should be interpreted as transparency about researcher degrees of freedom rather than a claim that each model is globally optimal.

\begin{table}[h]
\centering
\small
\caption{Key hyperparameters in the replication code (main run).}
\label{tab:hyper}
\begin{tabular}{p{0.28\textwidth}p{0.62\textwidth}}
\toprule
Component & Setting \\
\midrule
PM\textsubscript{2.5} cap (main) & 99.5th percentile of train sample \\
Lag features & 1, 2, 3, 24 hours \\
Calendar & hour, day-of-week, month \\
Heating season & October--December and January--April indicator \\
Ridge & $\alpha = 1$, least-squares solver \texttt{lsqr} \\
Random forest & 100 trees, max depth 10, \texttt{random\_state=42} \\
XGBoost & 100 estimators, max depth 6, $\eta=0.1$, \texttt{random\_state=42} (defaults; tuned values in JSON) \\
LightGBM & See repository defaults; Hyperopt can override (JSON) \\
AR benchmark & AR(24) with intercept, coefficients frozen after train fit \\
SARIMA (optional) & Order search via information criteria on train sample only \\
Tree tuning (optional) & Hyperopt (TPE), MAE on validation, max evals in config \\
\bottomrule
\end{tabular}
\end{table}

\section*{Acknowledgements}

The author thanks Rafayel Shirakyan for supervision and feedback on methodology and presentation.

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
