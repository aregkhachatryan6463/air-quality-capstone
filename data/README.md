# Data layout

- `raw/Air Quality Data/` — downloaded by `download_data()` (see repo root `download_data.py` and `DATA_README.md`).
- `processed/` — written by the pipeline: aligned city series, feature table, optional district aggregations.

See [DATA_README.md](../DATA_README.md) for field descriptions and source notes.
