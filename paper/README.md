# Manuscript outputs

- `figures/` — High-resolution plots from `python yerevan_pm25_forecasting.py --paper`
- `tables/` — LaTeX tables from `python export_paper_assets.py` (use `\usepackage{booktabs}` in the preamble)
- `bundle/` — Test-set predictions (local only; not committed to git)

See `docs/METHODOLOGY.md` for a concise description of the pipeline.
