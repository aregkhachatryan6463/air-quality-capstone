# Optional scripts

- `data_quality_supplement.py` — legacy exploratory figures under `figures_data_quality/` (simple ffill/bfill on the raw grid; for narrative contrast with the main pipeline’s **controlled** imputation in `src/preprocessing/imputation.py`). Run from the project root: `python scripts/data_quality_supplement.py` after data are downloaded.
