# Notebooks (optional)

The official reproducible entrypoint is **`run_pipeline.py`** at the repository root.

To explore results interactively, you may add Jupyter notebooks in this directory (e.g. load `results/tables/forecast_results_1h.csv` and `results/json/results_summary.json`). The pipeline does not require notebooks to run.
